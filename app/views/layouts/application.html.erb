<!DOCTYPE html>
<html>
<head>
  <title>Tfgjorge</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js","data-turbolinks-eval" => false %>
   <!-- Ionicons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/ionicons/2.0.1/css/ionicons.min.css">

  <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
  <%= csrf_meta_tags %>
  <%= csrf_meta_tags %>
</head>
<% if current_user && current_user.admin? %>
  <%= render 'layouts/adminlayout' %>

<% elsif current_user %>
  <body class="hold-transition skin-blue layout-top-nav" style="height: auto;">
    <div class="wrapper">
      <!-- Main Header -->
      <header class="main-header">
        <!-- Logo -->
        <!-- Header Navbar -->
        <nav class="navbar navbar-static-top">
          <div class="container">
            <div class="navbar-header">
              <a href="/" class="navbar-brand"><b>AppJorge</b>TFG</a>
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
                <i class="fa fa-bars"></i>
              </button>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse pull-left" id="navbar-collapse">
              <ul class="nav navbar-nav">
                <li class="active"><a href="#">Link <span class="sr-only">(current)</span></a></li>
                <li>
                  <%= link_to users_mycompetitions_path(current_user.id) do %>
                      <i class="fa fa-flag-checkered"></i>  My Competitions
                  <% end %>
                </li>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Dropdown <span class="caret"></span></a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="#">Action</a></li>
                    <li><a href="#">Another action</a></li>
                    <li><a href="#">Something else here</a></li>
                    <li class="divider"></li>
                    <li><a href="#">Separated link</a></li>
                    <li class="divider"></li>
                    <li><a href="#">One more separated link</a></li>
                  </ul>
                </li>
              </ul>
              <form class="navbar-form navbar-left" role="search">
                <div class="form-group">
                  <input type="text" class="form-control" id="navbar-search-input" placeholder="Search">
                </div>
              </form>
            </div>
            <!-- Navbar Right Menu -->
            <div class="navbar-custom-menu">
              <ul class="nav navbar-nav">
                <!-- User Account Menu -->
                <li class="dropdown user user-menu">
                  <!-- Menu Toggle Button -->
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <!-- The user image in the navbar-->
                    <!-- The user image in the navbar-->
                    <% if current_user.avatar? %>
                      <%= image_tag current_user.avatar.url, class: "user-image", :alt => "User Image" %>
                    <% else %>
                      <%= image_tag "/uploads/usuario_sin_foto.jpg", class: "user-image", :alt => "User Image"%>
                    <% end %>
                    <!-- hidden-xs hides the username on small devices so only the image appears. -->
                    <% if current_user %>
                      <span class="hidden-xs">Connect as <b><%= current_user.username %></b></span>
                    <% end%>
                  </a>
                  <ul class="dropdown-menu">
                    <!-- The user image in the menu -->
                    <li class="user-header">
                      <% if current_user.avatar? %>
                        <%= image_tag current_user.avatar.url, class: "img-circle imgresize", :alt => "User Image" %>
                      <% else %>
                        <%= image_tag "/uploads/usuario_sin_foto.jpg", class: "user-image", :alt => "User Image"%>
                      <% end %>
                      <p>
                        <%= current_user.name %> - User
                        <small>Member since <%= current_user.created_at.strftime("%d/%m/%Y") %></small>
                      </p>
                    </li>
                    <!-- Menu Body -->
                    <li class="user-footer">
                      <div class="pull-left">
                        <%= link_to "Profile", user_path(current_user.id), class: "btn btn-default btn-info", style: "color: white;" %>
                      </div>
                      <div class="pull-right">
                        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-default btn-danger", style: "color: white;" %>
                      </div>
                    </li>
                    <!-- Menu Footer-->
                    <li class="user-body">
                      <div class="row">
                        <div class="col-xs-4 text-center">
                          <i class="fa fa-flag-checkered"></i>
                          <%= link_to "Competitions", users_mycompetitions_path(current_user.id) %>
                        </div>
                        <div class="col-xs-4 text-center">
                          <i class="fa fa-edit"></i><br>
                          <%= link_to "Edit Profile", edit_user_path(current_user.id) %>
                        </div>
                        <div class="col-xs-4 text-center">
                          <i class="fa fa-gear"></i><br>
                          <%= link_to "Settings", edit_user_registration_path %>
                        </div>
                      </div>
                      <!-- /.row -->
                    </li>
                  </ul>
                </li>
              </ul>
            </div>
            <!-- /.navbar-custom-menu -->
          </div>
          <!-- /.container-fluid -->
        </nav>
      </header>

      <!-- Content Wrapper. Contains page content -->
      <div class="content-wrapper" style="min-height: 512px;">
        <div class="container">
          <!-- Content Header (Page header) -->
          <section class="content-header">
            <ol class="breadcrumb">
              <li><a href="#"><i class="fa fa-dashboard"></i> Level</a></li>
              <li class="active">Here</li>
            </ol>
          </section>
          <!-- Main content -->
          <section class="content">
            <!-- Your Page Content Here -->
            <div class="container">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </section>
          <!-- /.content -->
        </div>
        <!-- /.content-wrapper -->

        <!-- Main Footer -->
        <footer class="main-footer" id="footer">
          <div class="container">
            <!-- To the right -->
            <div class="pull-right hidden-xs">
              Trabajo Fin de Grado. V. 0.5
            </div>
            <!-- Default to the left -->
            <strong>Copyright &copy; 2016 <a href="#">Jorge Vela</a>.</strong> All rights reserved.
          </div>
          <!-- /.container -->
        </footer>
        <!-- Add the sidebar's background. This div must be placed
             immediately after the control sidebar -->
        <div class="control-sidebar-bg"></div>
      </div>
      <!-- ./container -->
    </div>
    <!-- ./wrapper -->


  </body>

<% else %>
  <%= render 'layouts/messages' %>
  <%= yield %>
<% end %>


<script type="text/javascript" src="https://cdn.jsdelivr.net/bootstrap.filestyle/1.1.0/js/bootstrap-filestyle.min.js"> </script>


</html>



